export enum authType {
  "菜单" = 1,
  "操作",
}

export enum yesOrNo {
  "否" = 0,
  "是" = 1,
}

export enum roleType {
  "禁用" = 0,
  "启用" = 1,
}

export enum userStatus {
  "关闭" = 0,
  "启用",
}

export enum oemType {
  "全贴牌",
}
